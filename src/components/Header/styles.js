import styled from 'styled-components/native';

export const Notch = styled.SafeAreaView`
  background-color: rgba(0, 0, 0, 0.3);
  flex: 0;
`;

export const Container = styled.View`
  background-color: rgba(0, 0, 0, 0.3);
  padding: 20px 0;
  align-items: center;
  justify-content: center;
`;

export const Logo = styled.Image`
  width: 23px;
  height: 24px;
`;
